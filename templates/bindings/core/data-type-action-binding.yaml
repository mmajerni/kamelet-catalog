- route:
    from:
      uri: "kamelet:timer-source"
      parameters:
        period: 1000
        message: "{ \"foo\": \"John\"}"
    steps:
      - to:
          uri: "kamelet:data-type-action"
          parameters:
            format: "The Data Type Format"
      - to:
          uri: "log:info"
