apiVersion: camel.apache.org/v1
kind: Pipe
metadata:
  name: slack-uri-binding
spec:
  source:
    ref:
      kind: Kamelet
      apiVersion: camel.apache.org/v1
      name: slack-source
    properties:
      serverUrl: yaks:resolveURL('slack-service')
      channel: ${slack.channel}
      token: ${slack.token}
  sink:
    uri: log:info
